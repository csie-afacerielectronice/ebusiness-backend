# ie-x-ebusiness-backend

## How to run

### Copy the example .env and config it

``` cp .env.example .env ```

### Install dependencies

``` npm install ```

### Migrate DB

``` npm run db:migrate ```

### Run it in dev mode

``` npm run dev ```
